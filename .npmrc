# React Native / Expo need a flat node_modules layout to resolve native modules.
node-linker=hoisted
# Keep the hoist pattern permissive so Metro can find every transitive dep.
public-hoist-pattern[]=*
