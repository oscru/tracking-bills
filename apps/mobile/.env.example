# Copy to .env.local and fill from `pnpm db:status` (local) or your Supabase
# project settings (hosted). Only EXPO_PUBLIC_* vars are exposed to the app.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
