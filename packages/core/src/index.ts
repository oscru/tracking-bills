/**
 * @repo/core — UI-agnostic business logic.
 *
 * Import the focused entry points directly, e.g.
 *   import { supabase } from '@repo/core/supabase';
 *   import { useTransactions } from '@repo/core/hooks';
 */
export * from './types';
export * from './validators';
export * from './utils';
export * from './i18n';
